@AGENTS.md

Claude-specific notes:

- Before pushing anything, re-read the SHARED REMOTE guardrail in AGENTS.md — an unrelated project (Colendi Ontology) shares `btekmen/memexlab-engine`.
- Test commands need the `PYTHONPATH=src` prefix (editable `.pth` not applied on this machine).
